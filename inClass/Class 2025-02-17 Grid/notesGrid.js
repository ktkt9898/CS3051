/* 
The children are the contents in the grid, not the actual grid cells.

Justify-items will center the children in the grid, but it will not center the grid itself. Notice the right side is centered, 
but the left side is not.

Align-items-center will center the grid itself, but it will not center the children in the grid. Notice the left side is centered,
Centers items vertically

Order of container elements do not matter, since CSS container lays out the grid based on the grid-template-areas.

To center the header, use justify-self and align-self.

Assignment take a header, left side menu, footer, and a center content area. Use grid to layout the page.
Something in the footer, like a copyright symbol
Menu, shows home, likes (favorite website), and places (tables)
Menu should underline the current page. Put all the elements in the DOM, but only show the currently selected page.

rem is the browser default, em is the parent size
Use vw and vh for responsive design for assignment
Make the menu a drop down list in the header or 3 row button in the header

Midterm: grid, flexbox, javascript forms
*/